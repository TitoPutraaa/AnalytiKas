import 'package:flutter/material.dart';

class Homeriwayat extends StatelessWidget {
  const Homeriwayat({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Home Riwayat", style: TextStyle(fontSize: 40));
  }
}
