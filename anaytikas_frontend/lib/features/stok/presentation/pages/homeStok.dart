import 'package:flutter/material.dart';

class Homestok extends StatelessWidget {
  const Homestok({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Home Stok", style: TextStyle(fontSize: 40));
  }
}
